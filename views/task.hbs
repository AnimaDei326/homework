<!DOCTYPE html>
<html lang="ru">
    <head>
        <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
        <link rel="stylesheet" href="style.css">
        <title>{{title}}</title>
    </head>
    <body>
        <div class="task">
            <a href="/">на главную страницу</a><br>
            <a href="/add">создать задачу</a>
        </div>
        {{#each rows}}
            <div class="task">
                <h3>Задача № {{this.id}}</h3>
                <h3>Статус: {{#if this.done}} выполнена {{else}} не выполнена {{/if}} </h3>
                <h3>Описание: {{this.text}}</h3>
                <a href="/edit/{{this.id}}">редактировать</a><br>
                <a href="/delete/{{this.id}}">удалить</a>
            </div>
        {{/each}}
    </body>
</html>